language: swift

before_install: brew upgrade swiftlint

install:
- ./install_swiftlint.sh

script:
- swiftlint
- xcodebuild -scheme travis-ci-exTests -workspace travis-ci-ex.xcworkspace -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 8 Plus,OS=11.1' -enableCodeCoverage YES build test
- bash <(curl -s https://codecov.io/bash) -cF ios
